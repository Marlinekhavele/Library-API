### Library API
- A rest api in Django and DRF For managing a library
- Clone the project using git clone
- Enter the project directory  cd app
- While inside the app directory run docker-compose build to build the containers
- Start the project with the command docker-compose up 
- Visit http://localhost:8000/api/v1/docs to get started with the browsable api
